# Copy to ~/.hermes/.env (home-manager creates this on first switch if missing).
# GATEWAY_PROXY_KEY must match API_SERVER_KEY in the shipyards hermes-secret.
GATEWAY_PROXY_URL=https://hermes-api.andstu.xyz
GATEWAY_PROXY_KEY=changeme
